import { VitNodeConfig } from 'vitnode/vitnode.config';

export const vitNodeConfig: VitNodeConfig = {
  metadata: {
    title: 'VitNode',
    shortTitle: 'VitNode',
  },
  plugins: [],
  debug: true,
};
